---
import Logo from "@/assets/logo.svg";
import LocaleSelect from "@/components/i18n/LocaleSelect.astro";
import { type Lang } from "@/i18n";

const locale = Astro.currentLocale as Lang;
const currentPath = Astro.url.pathname;
const pageList = ["blog", "works", "me", "price"];
---

<nav class="l-nav">
  <h1 class="logo">
    <a href={`/${locale}/`}>
      <img src={Logo.src} width={Logo.width} height={Logo.height} alt="WEBA" />
    </a>
  </h1>

  <ul class="menu">
    {
      pageList.map((path) => {
        const isActive = new RegExp(`^/${locale}/${path}/?$`).test(currentPath);
        const isChild = new RegExp(`^/${locale}/${path}(?:/|$)`).test(
          currentPath,
        );

        return (
          <li>
            {isChild && (
              <div class="navIndicator" transition:name="nav-indicator" />
            )}

            <a class={isChild ? "active" : null} href={`/${locale}/${path}`}>
              <dotlottie-player
                autoplay={isActive ? "autoplay" : null}
                src={`/lottie/${path}.lottie`}
              />
              {path.toUpperCase()}
            </a>
          </li>
        );
      })
    }
  </ul>

  <a
    class:list={[
      "contact",
      { active: new RegExp(`^/${locale}/contact/?$`).test(currentPath) },
    ]}
    href={`/${locale}/contact`}
  >
    <dotlottie-player
      autoplay={/^\/contact\/?$/.test(currentPath) ? "autoplay" : null}
      src="/lottie/contact.lottie"></dotlottie-player>
    CONTACT
  </a>

  <div class="selectContainer">
    <LocaleSelect />
  </div>
</nav>

<style>
  nav {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    gap: 48px;
    color: #fff;
    @media (width < 1280px) {
      gap: 32px;
    }
    @media (width < 1024px) {
      align-items: start;
      padding: 28px 8px 0;
      gap: 8px;
    }
    @media (width < 480px) {
      padding: 20px 10px 0;
    }
  }

  dotlottie-player {
    width: 36px;
    height: 36px;
    @media (width < 480px) {
      width: 32px;
      height: 32px;
    }
  }


  .logo {
    width: 168px;
    margin-block-start: -5vh;
    line-height: 1;
    @media (width < 1024px) {
      margin-block-start: 0;
      width: 128px;
    }
    @media (width < 480px) {
      width: 96px;
    }

    a {
      display: block;
      opacity: 0.9;
      transform-origin: left center;
      &:hover,
      &:active {
        opacity: 1;
        scale: 1.02;
      }
    }
    span {
      display: block;
      height: 0;
    }
  }

  .menu,
  .contact {
    font-family: var(--font-futura), sans-serif;
    font-weight: 500;
    font-variation-settings: "wght" 500;
  }

  .menu {
    width: 168px;
    @media (width < 1024px) {
      display: flex;
      width: 100%;
      justify-content: space-between;
      align-items: center;
      padding: 0 16px;
    }
    @media (width < 480px) {
      padding: 0 24px;
    }

    li {
      position: relative;
      margin-block-start: 16px;

      .navIndicator {
        position: absolute;
        right: 105%;
        bottom: 12px;
        width: 12px;
        height: 16px;
        clip-path: polygon(0 0, 100% 50%, 0 100%);
        background-color: #fff;
        transition: var(--transition);
        @media (width < 1024px) {
          width: 48px;
          height: 4px;
          bottom: -8px;
          right: calc(50% - 24px);
          clip-path: none;
          border-radius: 4px 4px 0 0;
        }
        @media (width < 480px) {
          width: 32px;
          right: calc(50% - 16px);
        }
      }
    }

    a {
      display: flex;
      height: 40px;
      align-items: center;
      gap: 6px;
      font-size: 20px;
      opacity: 0.8;
      text-shadow: 0 0 24px rgb(136 68 0 / 0.2);
      @media (width < 1024px) {
        font-size: 18px;
      }
      @media (width < 640px) {
        font-size: 16px;
        gap: 2px;
      }
      @media (width < 480px) {
        flex-direction: column;
        height: auto;
        gap: 0;
        font-size: 11px;
      }
      &.active,
      &:hover,
      &:active {
        opacity: 1;
        font-variation-settings: "wght" 700;
      }
    }
  }

  .contact {
    width: 168px;
    display: flex;
    gap: 6px;
    height: 48px;
    justify-content: center;
    align-items: center;
    font-size: 18px;
    background: rgb(255 255 255 / 0.4);
    /* text-shadow: 0 0 24px rgb(136 68 0 / 0.2); */
    word-break: keep-all;
    border: 2px solid rgb(255 255 255 / 0.4);
    border-radius: 12px;
    box-shadow: 0 12px 28px -10px rgb(0 0 0 / 0.1);
    backdrop-filter: blur(1px);
    font-size: 1em;
    @supports (corner-shape: superellipse(2.4)) {
      corner-shape: superellipse(2.4);
      border-radius: 32px;
    }
    &.active,
    &:hover,
    &:active {
      font-variation-settings: "wght" 700;
      filter: drop-shadow(0 0 8px rgb(255 255 255 / 0.5));
    }
    @media (width < 1024px) {
      width: 140px;
      height: 40px;
      position: absolute;
      top: 24px;
      right: 2px;
      font-size: 1em;
      dotlottie-player {
        margin-inline-start: -6px;
      }
    }
    @media (width < 480px) {
      width: 64px;
      top: 12px;
      right: 6px;
      text-indent: 200%;
      line-height: 10;
      display: block;
      overflow: hidden;
      dotlottie-player {
        margin: 4px auto;
      }
    }
  }

  .selectContainer {
    width: 168px;
    @media (width < 1024px) {
      width: auto;
      position: absolute;
      top: 24px;
      right: 152px;
    }
    @media (width < 480px) {
      top: 14px;
      right: 80px;
    }
  }
</style>
